CREATE TABLE [dbo].[offlinx_device_type] (
    [offlinx_device_type_code] CHAR (4)     NOT NULL,
    [name]                     VARCHAR (50) NOT NULL,
    PRIMARY KEY CLUSTERED ([offlinx_device_type_code] ASC)
);

