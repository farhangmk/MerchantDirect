CREATE TABLE [dbo].[offlinx_attribution_model] (
    [offlinx_attribution_model_code] CHAR (4)     NOT NULL,
    [name]                           VARCHAR (50) NOT NULL,
    PRIMARY KEY CLUSTERED ([offlinx_attribution_model_code] ASC)
);

